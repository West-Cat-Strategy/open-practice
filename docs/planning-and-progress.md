# Planning and Progress

**Last Updated:** 2026-05-11

Use this file for live tracked work, immediate next moves, and the forward-looking development plan.
Use `docs/planning.md` for the durable roadmap, `docs/improvement-opportunities.md` for candidate
backlog ideas, and `docs/planning-completed-archive.md` for historical validation proof.

## At a Glance

| Snapshot              | Value                                                                                      |
| --------------------- | ------------------------------------------------------------------------------------------ |
| Batch convergence     | OP-M16 completed the saved queue-view follow-up on top of the OP-M13-OP-M15 adoption wave. |
| Next recommended pick | No active candidate rows after the OP-T42 reporting closeout.                              |
| Ready rows            | 0                                                                                          |
| Candidate rows        | 0                                                                                          |
| In progress rows      | 0                                                                                          |
| Review rows           | 0                                                                                          |
| Blocked rows          | 0                                                                                          |
| Completed archive     | Historical proof moved to [Planning Completed Archive](planning-completed-archive.md).     |
| Status vocabulary     | `Ready`, `Candidate`, `In Progress`, `Review`, `Blocked`, `Done`                           |

## Forward Development Plan

| Status | ID    | Task             | Immediate Next Move                                                                                                 | Validation Plan                                                                            |
| ------ | ----- | ---------------- | ------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| _None_ | _N/A_ | _No active rows_ | _Add the next concrete slice from `docs/planning.md` or `docs/improvement-opportunities.md` before implementation._ | _Run `pnpm verify:select -- --files <changed paths...>` after the next slice is selected._ |

## Recently Completed

| ID            | Outcome                         | Concise Proof                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------- | ------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OP-T42        | Jurisdiction trust report       | Completed a narrow read-only jurisdiction-aware trust report on top of the existing trust controls/reconciliation data: the domain builder aggregates accessible matter jurisdiction labels, trust balances, approval counts, reconciliation exception counts, statement-row counts, variance totals, unreconciled accounts, and overdrawn diagnostics; the API exposes firm-wide `GET /api/ledger/reports/jurisdictional-trust?jurisdiction=` without evidence, private statement-row details, exports, mutations, persistence, payment behavior, automatic trust posting, or certification claims; the Funds dashboard shows the active matter jurisdiction summary with operator-review-only and not-jurisdiction-certified copy. 2026-05-11 proof: `pnpm verify:select -- --files $(git diff --name-only)` recommended format/docs/policy, domain/API/web tests and typechecks, provider/worker tests, and build; passed focused ledger/domain/API/web tests, full domain/API/web/provider/worker tests, domain/API/web typechecks, `pnpm format:check`, `pnpm docs:check`, `pnpm policy:check`, `pnpm build`, and `git diff --check`. No database paths changed, so `db:check` was not selected. |
| OP-T41        | Fiscal-host metadata slice      | Completed the smallest honest fiscal-host metadata/dashboard slice without new persistence, migrations, report exports, accounting claims, or trust-ledger posting: the pure selector now whitelists fiscal-host program metadata and restricted-fund metadata from existing legal-clinic records, drops malformed/extra metadata, and the dashboard renders read-only fiscal-host/restricted-fund status cues in existing matter and intake clinic surfaces. 2026-05-11 proof: selector on the full dirty path set recommended format/docs/policy, domain/API/provider/worker/web tests, domain/API/web typechecks, and build; passed `pnpm format:check`, `pnpm docs:check`, `pnpm policy:check`, `pnpm --filter @open-practice/domain test`, `pnpm --filter @open-practice/domain typecheck`, `pnpm --filter @open-practice/api test`, `pnpm --filter @open-practice/api typecheck`, `pnpm --filter @open-practice/providers test`, `pnpm --filter @open-practice/worker test`, `pnpm --filter @open-practice/web test`, `pnpm --filter @open-practice/web typecheck`, and `pnpm build`.                                                                                                           |
| OP-M16        | Saved queue view apply behavior | Saved queue definitions on the queue dashboard can now be applied and cleared after creation: the panel reuses the existing `/api/operational-views/definitions?surface=queues` data, filters already-authorized queue sections by saved `filters.queueSections`, applies the saved `rowLimit`, and keeps archive behavior intact. 2026-05-11 proof: selector on the full dirty path set, including concurrent OP-T41/OP-M17 files, recommended format/docs/policy, domain/API/providers/worker/web tests, domain/API/web typechecks, and build; passed `pnpm format:check`, `pnpm docs:check`, `pnpm policy:check`, `pnpm --filter @open-practice/domain test`, `pnpm --filter @open-practice/domain typecheck`, `pnpm --filter @open-practice/api test`, `pnpm --filter @open-practice/api typecheck`, `pnpm --filter @open-practice/providers test`, `pnpm --filter @open-practice/worker test`, `pnpm --filter @open-practice/web test`, `pnpm --filter @open-practice/web typecheck`, and `pnpm build`.                                                                                                                                                                                          |
| OP-M17        | Intake review dashboard         | Finished the submitted intake review lifecycle on the web dashboard: pending submitted form links now expose a staff review loader, summarize the matter-scoped answer snapshot and item actions, support accept/reject/request-more-info decisions against the landed API contract, require reasons for reject/request-more-info, and surface one-time follow-up token/URL output only from the create-time request-more-info response. 2026-05-11 proof: selector on changed intake web/workboard paths recommended `pnpm format:check`, `pnpm docs:check`, `pnpm policy:check`, web test, web typecheck, and `pnpm build`; passed targeted dashboard-client test, full `pnpm --filter @open-practice/web test`, `pnpm --filter @open-practice/web typecheck`, `pnpm build`, `pnpm format:check`, `pnpm docs:check`, and `pnpm policy:check`.                                                                                                                                                                                                                                                                                                                                                       |
| OP-M13-OP-M15 | Adoption wave                   | Completed the owner-authorized nonprofit-manager adoption wave with Open Practice-native implementation: public intake forms now support token-scoped draft save/reload, public autosave/manual save state, and non-editable submitted/review/request-more-info receipt behavior while preserving `clientSubmissionId` submit idempotency; saved operational view definitions now have private firm-scoped CRUD/archive APIs plus first queue-dashboard UI adoption with permission-scope filtering; worker health now exposes a read-only `/api/jobs/health` summary and dashboard status without job payloads or queue controls. 2026-05-10 proof: selector on all changed API/domain/database/web/docs/migration paths recommended policy, database tests, database `db:check`, database typecheck, API tests, and API typecheck; passed `pnpm format:check`, domain/database/API/web tests and typechecks, database `db:check`, `pnpm docs:check`, `pnpm policy:check`, `pnpm build`, and `git diff --check`.                                                                                                                                                                                     |
| OP-M12        | Pattern adoption pass           | Completed the clean-room nonprofit-manager behavioral adoption slice without importing nonprofit-manager module layout or implementation code: public intake submit now supports `clientSubmissionId` idempotency with fingerprint reservation before snapshot/proposal writes, exact submitted replays, stable `INTAKE_FORM_SUBMISSION_CONFLICT` conflicts, and no duplicate snapshots/proposals; public token web helpers centralize path building, status-only errors, and shared loading/error/empty shells; intake schema-v2 rendering now flows through a shared renderer for the public runner; route catalog tests lock a small owner/reviewer/billing/restricted legal workflow matrix; candidate-only nonprofit-manager-inspired backlog ideas are tracked in `docs/improvement-opportunities.md`. 2026-05-10 proof: selector on changed API/domain/database/web/docs paths recommended format/docs/policy, domain/database/API/web/provider/worker tests and typechecks, database `db:check`, and build; all passed.                                                                                                                                                                       |
| OP-M8-OP-M11  | Cleanup pass                    | Completed the coordinated behavior-preserving cleanup pass: `packages/database/src/repository.ts` is now a public facade over focused contract, mapper, memory, and Drizzle implementation modules; `packages/database/test/repository.test.ts` is split into focused repository behavior test files while keeping the activity/redaction test intact; `apps/web/app/dashboard-client.tsx` now delegates render-only dashboard shell, matter overview, and shared panel pieces while retaining state/mutation ownership; `apps/web/app/styles.css` is an ordered import manifest over extracted global CSS chunks with cascade order preserved. 2026-05-10 proof: selector on changed web/database/docs paths recommended database tests, database `db:check`, database typecheck, API tests, web tests, web typecheck, and build; those checks passed, along with API typecheck, `pnpm format:check`, `pnpm docs:check`, `pnpm policy:check`, and `git diff --check`.                                                                                                                                                                                                                                |
| OP-M7         | Multi-lane operations pass      | Completed a coordinated nonprofit-manager-inspired pass without copying nonprofit-manager implementation structure: public token routes now use route-local view/mutation/upload rate limits without exposing token-bearing URLs in limit keys; submitted public intake now links to answer snapshots, creates review-safe task/queue signals, supports staff accept/reject/request-more-info decisions, and returns follow-up raw tokens once; the dashboard loads existing operational views into the operations focus panel using aggregate labels/counts only; validation selector and boundary scripts now have contract tests plus explicit `--dirty` and opt-in `--strict` modes. 2026-05-10 proof: selector across API/domain/database/web/scripts/docs/package paths recommended `pnpm ci:local` plus focused package checks; `node --test scripts/*.test.mjs`, domain/database/API/web/providers/worker tests and typechecks, database `db:check`, `pnpm format:check`, `pnpm docs:check`, `pnpm policy:check`, `pnpm test`, `pnpm build`, and `pnpm ci:local` passed.                                                                                                                      |

See [Planning Completed Archive](planning-completed-archive.md) for the full historical workboard and validation ledger.

## Coordination Rules

- Update this file before starting tracked work if status, owner, blocker, or immediate next move changes.
- Keep rows concrete; do not add summary-only tasks.
- Keep implementation rows scoped to one subsystem unless a coordinated exception is recorded here.
- Promote candidate work to `Ready` only when the first slice, owning paths, and validation plan are clear.
- Record fresh validation output before moving a reviewed row to `Done`.
- Preserve completed validation proof in [Planning Completed Archive](planning-completed-archive.md) instead of the active board.
- Do not edit `/Users/bryan/projects/nonprofit-manager` from this workboard.
